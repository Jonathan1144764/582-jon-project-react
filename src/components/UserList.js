export default function UserList(props) {
  <>
    <label for="users">Login: </label>
    <select name="users" id="users-list">
      <option value=""></option>
      <option></option>
    </select>
  </>;
}
